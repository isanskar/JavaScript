const width = +prompt('Enter width of rectangle: ')
const height = +prompt('Enter height of rectangle: ')

console.log(`Area of rectange is ${width * height}`)
document.write(`Area of rectange is ${width * height}`)